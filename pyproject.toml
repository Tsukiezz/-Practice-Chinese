[project]
name = "hanzigo"
version = "1.0.0"
requires-python = ">=3.13,<3.14"
dependencies = [
  "fastapi==0.115.6",
  "uvicorn==0.34.0",
  "httpx==0.28.1",
  "python-dotenv==1.1.1",
  "Pillow>=11.0,<13",
  "libsql==0.1.11",
]

[tool.vercel]
entrypoint = "api.index:app"
